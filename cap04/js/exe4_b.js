const frm = document.querySelector("form")
const resp = document.querySelector("h3")

frm.addEventListener("Submit",(t)=>{
    t.preventDefault()
    const veloPer = Number(frm.inVelPermit.value)
    const velPerm = Number(frm.inVelCondu.value)
     if(){
        
     }
})